# Environment example for Tarot API
# Copy to .env and adjust values per environment

# Runtime
ENV=local                 # local | dev | prod
LOG_LEVEL=INFO

# CORS (JSON list). In dev/prod, must not be empty. Example:
# CORS_ORIGINS=["https://your-cloudflare-pages-domain.pages.dev","https://www.example.com"]

# Data
DATA_PATH=data/tarot-images.json
# Optional meanings file (i18n-ready)
MEANINGS_PATH=

# Rate limits
RATE_LIMIT_DEFAULT=60/minute
RATE_LIMIT_HEALTH=5/second
RATE_LIMIT_CARDS=120/minute
RATE_LIMIT_READING_POST=10/minute

# Database (optional)
USE_DB=false
DB_URL=postgresql://user:password@host:5432/dbname
